/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatars.githubusercontent.com'], // Add the GitHub avatars domain
    },
  };
  
  export default nextConfig;
  